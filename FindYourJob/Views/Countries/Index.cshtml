@model IEnumerable<FindYourJob.Models.Country>

@{
    ViewBag.Title = "Index";
}


<div id="titlebar" class="gradient">
    <div class="container">
        <div class="row">
            <div class="col-xl-12">

                <h2></h2>

                <!-- Breadcrumbs -->
                <nav id="breadcrumbs" class="dark">
                    <ul>
                        <li><a href="#"></a></li>
                        <li><a href="#"></a></li>
                        <li></li>
                    </ul>
                </nav>

            </div>
        </div>
    </div>
</div>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Country1)
        </th>
        <th></th>
    </tr>

    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Country1)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.CountryID }) |
                @Html.ActionLink("Details", "Details", new { id = item.CountryID }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.CountryID })
            </td>
        </tr>
    }

</table>
